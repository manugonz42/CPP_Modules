#pragma once

#include <string>
#include <iostream>
#include <cstdlib>

using std::cout;
using std::string;
using std::endl;

class   Base{
    protected:
        Base() {}
    public:
        virtual ~Base() {}
};